---
version: "2.0"
name: "דיוק הנדסי — מערכת עיצוב לספר מתמטיקה לכיתה ט׳"
description: "מערכת עיצוב לספר לימוד דיגיטלי אדפטיבי, לכל יחידות תוכנית הלימודים של כיתה ט׳. אסתטיקה תעשייתית מדויקת: רשתות הנדסיות, טיפוגרפיה טכנית, מינימליזם מובנה. עברית RTL, ללא תלויות חיצוניות."
scope: "ספר שלם — רב-יחידתי. שום החלטה עיצובית אינה נגזרת מנושא לימודי מסוים."
direction: rtl
locale: he-IL
density: 3/10 — אוורירי
variance: 2/10 — מובנה
motion: 4/10 — מרוסן
colorScheme: light-only
colors:
  primary: "#002D62"
  primaryAlt: "#003366"
  accent: "#00BFFF"
  ink: "#36454F"
  silver: "#C0C0C0"
  border: "#E0E0E0"
  surface: "#FFFFFF"
  ground: "#F7F8FA"
  success: "#008000"
  warning: "#FFA500"
  danger: "#A32036"
typography:
  hero:
    fontFamily: Assistant
    fontSize: clamp(2.5rem, 5vw, 4rem)
    fontWeight: 700
  h1:
    fontFamily: Assistant
    fontSize: 2.25rem
    fontWeight: 700
  body-md:
    fontFamily: Assistant
    fontSize: 1rem
    fontWeight: 400
    lineHeight: 1.65
  math:
    fontFamily: Cambria Math
    fontSize: 1.05em
  mono:
    fontFamily: JetBrains Mono
    fontSize: 0.875rem
components:
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.surface}"
    padding: 0.6rem 1.1rem
    radius: 8px
---

## סקירה

מערכת עיצוב לספר לימוד דיגיטלי אדפטיבי במתמטיקה לכיתה ט׳ — **לכל יחידות
תוכנית הלימודים של השנה**, לא ליחידה אחת. הכיוון החזותי הוא **דיוק הנדסי**:
תעשייתי, מדויק, מינימליסטי. הרעיון המרכזי — מתמטיקה היא מערכת מובנית עם
כללים חדים, והממשק צריך להרגיש כך: רשת, סדר, אפס רעש דקורטיבי.

- **סגנון:** תעשייתי, מדויק, מינימליסטי
- **מילות מפתח:** דיוק, הנדסה, מבנה, אמינות, יעילות, חדר נקי, שקיפות
- **תקופה:** 2026+
- **מצב צבע:** בהיר בלבד. כל הטוקנים מוגדרים כמשתנים, כך שאפשר להוסיף מצב
  כהה בהמשך בלי לשנות מבנה.

### כלל העל של המסמך הזה

> **שום החלטה עיצובית לא נגזרת מנושא לימודי מסוים.**
> כל מה שתלוי-נושא מוגדר כאן כ*חוזה עם מקום פנוי*, ויחידה חדשה ממלאת אותו.
> הוספת יחידה לספר לעולם אינה דורשת צבע חדש, גופן חדש, רכיב חדש או חריגה
> מהמערכת.

### שתי התאמות מכוונות למקור הסגנון

**המוטיב.** מקור הסגנון מדבר על דיאגרמות מעגלים ואנימציות הרכבת שבבים. בספר
מתמטיקה זה מוטיב שאול וזר. שמרנו על ה*מבנה* — רשת הנדסית, טיפוגרפיה טכנית,
ויזואליזציית נתונים, ריסון — והחלפנו את הקישוט המוליכי-למחצה בחוזה של
**מודל חזותי לכל יחידה**. פירוט בסעיף מוטיבים.

**הגופן.** המקור מגדיר Roboto לכל הטקסט. הכיסוי העברי של Roboto חלקי ואיכות
העברית בו נמוכה. לכן העברית עוברת לגופן עברי ייעודי, ו-Roboto נשמר לתוויות
לטיניות ולמספרים טכניים בלבד.

---

## עקרונות מנחים

1. **המתמטיקה היא הגיבורה.** כל שאר האלמנטים מרוסנים כדי שהביטוי, השרטוט או
   הגרף יהיו הדבר הבולט ביותר על המסך.
2. **דיוק ולא קישוט.** אין גרדיאנטים דקורטיביים, אין צללים דרמטיים, אין
   אנימציות שמושכות תשומת לב. כל אפקט חייב להעביר מידע.
3. **המצב תמיד גלוי.** תלמיד/ה צריך/ה לדעת בכל רגע: מה נשלט, מה פתוח, מה
   נעול ולמה. נעילה בלי הסבר היא כישלון עיצובי.
4. **משוב נקרא לפני שהוא זז.** תשובה נכונה או שגויה מוצגת מיד וללא אנימציית
   כניסה. אנימציה על משוב מעכבת קריאה.
5. **הספר גדל, המערכת לא.** יחידה חדשה נכנסת דרך החוזים שבמסמך הזה. אם היא
   דורשת חריגה — החוזה שגוי וצריך לתקן אותו, לא לחרוג ממנו.
6. **עובד בלי אינטרנט.** אפס CDN, אפס פונטים חיצוניים, אפס ספריות. רשתות
   בתי ספר חוסמות משאבים חיצוניים, ולומדה שנשברת בכיתה היא לומדה שלא בשימוש.

---

## ארכיטקטורת תוכן

```
ספר
 └─ יחידה              (כפל מקוצר ופירוק · פונקציות · גיאומטריה · …)
     └─ שלב            (קיבוץ בתוך יחידה: יסודות · נוסחאות · יישום)
         └─ מיומנות     (הפריט האטומי: יש לו שיעור, גנרטור ומדד שליטה)
```

**המערכת אגנוסטית למספרים.** אין הנחה על כמות היחידות, כמות השלבים ביחידה או
כמות המיומנויות בשלב. רשת המיומנויות זורמת (`auto-fill, minmax(215px, 1fr)`)
וגדלה לכל גודל. אין ספירה קשיחה בשום מקום בעיצוב.

### חוזה היחידה

כל יחידה חדשה מצהירה על ארבעה דברים, וזה **כל** מה שהיא רשאית להגדיר:

| # | הצהרה | דוגמה מיחידת האלגברה |
|---|---|---|
| 1 | **מודל חזותי קנוני** אחד | מודל שטח |
| 2 | **ערכת סמלים** לשדה התשובה | `x · x² · x³ · xⁿ · ( ) + − · /` |
| 3 | **צורות תשובה** נדרשות | שקול · פתוח · מפורק · מצומצם · שורשים |
| 4 | **סוגי איורים** | אין |

יחידה **אינה** רשאית להגדיר: צבע משלה, גופן משלה, רדיוס משלה, פריסה משלה או
רכיב שלא קיים במערכת.

### דוגמאות למילוי החוזה

הטבלה להמחשה בלבד — היא **לא** רשימת היחידות של הספר. את הרשימה בפועל קובעת
תוכנית הלימודים, והיא נכנסת כשהיחידה נבנית.

| משפחת יחידות | מודל חזותי | סמלים נוספים | צורות תשובה | איורים |
|---|---|---|---|---|
| ביטויים אלגבריים | מודל שטח | `( ) ^` | שקול · פתוח · מפורק | — |
| משוואות ואי-שוויונות | מאזניים · ישר המספרים | `≤ ≥ ≠` | ערך · קבוצת פתרונות | ישר מספרים |
| פונקציות וגרפים | מערכת צירים | `( , )` | נקודה · ביטוי · תחום | גרף אינטראקטיבי |
| גיאומטריה | שרטוט מסומן | `√ ° ∠ ≅` | אורך · שטח · נימוק | שרטוט SVG |
| טריגונומטריה | משולש ישר-זווית | `sin cos tan °` | ערך + יחידה | שרטוט SVG |
| סטטיסטיקה והסתברות | טבלת שכיחויות | `% /` | מספר · שבר · אחוז | תרשים |
| יחס, אחוזים וקנה מידה | פסי השוואה | `: %` | יחס · ערך | תרשים פסים |

---

## צבעים

| טוקן | ערך | שימוש | ניגודיות על לבן |
|---|---|---|---|
| `--blue-deep` | `#002D62` | כותרות, טקסט ראשי, מילוי כפתור ראשי | 14.1:1 ✓ |
| `--blue-primary` | `#003366` | קישורים, מצבי פוקוס, הדגשה | 12.4:1 ✓ |
| `--electric` | `#00BFFF` | **לא לטקסט.** מחווני התקדמות, ויזואליזציה, זוהר פוקוס | 2.1:1 ✗ |
| `--ink` | `#36454F` | טקסט גוף | 9.9:1 ✓ |
| `--silver` | `#C0C0C0` | **לא לטקסט.** קווי רשת, מסגרות דקורטיביות | 1.8:1 ✗ |
| `--border` | `#E0E0E0` | מסגרות, מפרידים | — |
| `--surface` | `#FFFFFF` | רקע כרטיסים | — |
| `--ground` | `#F7F8FA` | רקע העמוד | — |
| `--success` | `#008000` | תשובה נכונה, מיומנות נשלטת | 5.1:1 ✓ |
| `--warning` | `#FFA500` | **לא לטקסט.** מילוי בלבד; טקסט על גביו כהה | 1.9:1 ✗ |
| `--danger` | `#A32036` | תשובה שגויה, תפיסה מוטעית | 7.4:1 ✓ |

**שלושה צבעים אסורים לטקסט.** `--electric`, `--silver` ו-`--warning` נופלים
מתחת ל-4.5:1 על רקע לבן. הם למילוי, לקווים ולמחוונים בלבד. לטקסט "אזהרה"
השתמשו בגוון כהה: `--warning-ink: #8A5A00`.

**`--danger` הוא תוספת שלנו.** בפלטת המקור אין אדום כלל, ולומדה שחייבת להציג
תשובה שגויה לא יכולה לוותר עליו. נבחר גוון כהה ומרוסן שמשתלב בפלטה ועובר AA.

### כלל: צבע מקודד מצב, לא נושא

> **אין צבע ליחידה.** גיאומטריה אינה ירוקה ואלגברה אינה כחולה.

הפלטה כולה מוקצית למצבי לומד — נכון, שגוי, חלקי, נשלט, נעול. אם צבע יסמן גם
נושא, הוא יתנגש עם המצב ברגע שיחידה "ירוקה" תציג תשובה שגויה. יחידות מובחנות
זו מזו בשם, בכותרת השלב ובמיקום במפה — לא בגוון.

זה גם מה שמאפשר להוסיף יחידה חמש-עשרה בלי לחפש צבע פנוי.

### מיפוי סמנטי

```
תשובה נכונה             → success  + מסגרת #B8DCC6 + רקע #E4F3EA
תשובה שגויה             → danger   + מסגרת #F0C4CC + רקע #FBE9EC
נכון אבל לא בצורה שנדרשה → warning-ink + מסגרת #ECD9AB + רקע #FBF0DA
מיומנות נשלטת           → success
מיומנות פתוחה           → blue-primary
מיומנות נעולה           → silver על ground, אטימות 55%
תפיסה מוטעית פעילה      → danger על רקע מטושטש
פאנל המורה הפרטי        → blue-deep על #ECEBF8
```

**אף מצב לא מסומן בצבע בלבד.** לכל מצב יש גם אייקון וגם טקסט. תלמיד/ה עם
עיוורון צבעים חייב/ת להבין את המשוב.

---

## טיפוגרפיה

```css
--font-he:   "Assistant", "Rubik", "Heebo", "Segoe UI", system-ui, sans-serif;
--font-tech: "Roboto", "Assistant", system-ui, sans-serif;
--font-math: "Cambria Math", "Latin Modern Math", Georgia, "Times New Roman", serif;
--font-mono: "JetBrains Mono", ui-monospace, Consolas, monospace;
```

- **כותרות וגוף (עברית):** `--font-he`. משקל 400 לגוף, 650–700 לכותרות.
- **תוויות טכניות ומספרים:** `--font-tech`, 0.875rem, משקל 500, letter-spacing 0.01em.
- **ביטויים מתמטיים:** `--font-math`. סריף מרנדר מתמטיקה טוב משמעותית מגרוטסק —
  ההבחנה בין `x` ל-`×` ובין `1` ל-`l` ברורה יותר, וגם סימני שורש, זוויות
  ואותיות יווניות נראים נכון.
- **ערכים טכניים** (seed, מזהה תרגיל): `--font-mono`.

### סולם

| רמה | גודל |
|---|---|
| Hero | `clamp(2.5rem, 5vw, 4rem)` |
| H1 | 2.25rem |
| H2 | 1.5rem |
| גוף | 1rem / 1.65 |
| קטן | 0.875rem |
| **ביטוי בתרגיל** | 1.9rem (1.5rem במובייל) |

### כללי מתמטיקה שאין לעקוף

חלים על **כל** יחידה, בכל נושא:

1. **מעריכים תמיד בכתב עילי.** הסימן `^` הוא צורת *הקלדה* ולעולם לא מגיע
   למסך. `x^2` → x², `x^m` → xᵐ, `x^(m+n)` → x בעליון m+n.
2. **אינדקסים תמיד בכתב תחתי.** `a_1` → a₁. אותו כלל, כיוון הפוך.
3. **ביטוי מתמטי הוא אי LTR בתוך זרימה עברית.** כל ביטוי נעטף ב-`dir="ltr"`
   עם `unicode-bidi: isolate`. בלי זה `x - 3` מוצג הפוך. חל גם על נוסחאות,
   על תוויות בגרף ועל מידות בשרטוט.
4. **סוגריים בהקשר RTL משתקפים.** תווית כפתור שכתוב בה `(` תצויר על המסך
   כ-`)`. כל כפתור סמל חייב `dir="ltr"` — גם אם התו שנכנס לשדה נכון.
   הכלל חל על כל תו מראה: `( ) [ ] { } < >`.
5. **יחידות מידה נצמדות למספר.** `5 ס״מ` ולא `5ס״מ`, ורווח קשיח ביניהם.

---

## פריסה

- **רשת:** CSS Grid. יחידת בסיס 0.5rem (8px).
- **מכל רחב** (מפת הספר, דשבורד): מקסימום 1280px, ריפוד צד 1.5rem.
- **מכל קריאה** (עמוד שיעור): **68–72ch**. ספר נקרא, לא נסרק — שורה של
  1280px ברוחב מלא הורסת קריאוּת.
- **מכל איור** (גרף, שרטוט): עד רוחב מכל הקריאה, יחס גובה-רוחב שמור,
  ממורכז, עם `overflow-x: auto` משלו אם הוא רחב מדי במובייל.
- **מרווח בין מקטעים:** `clamp(3rem, 6vw, 6rem)`.
- **התמוטטות מובייל:** כל פריסה רב-טורית עוברת לטור אחד מתחת ל-768px.
  אפס גלישה אופקית בגוף העמוד.
- **חוזה z-index:** base 0 / sticky-nav 100 / overlay 200 / modal 300 / toast 500.

### RTL — תכונות לוגיות בלבד

```css
/* נכון */   padding-inline-start   margin-inline-end   inset-inline-start
/* אסור */   padding-left           margin-right        left
```

`text-align: start` ולא `right`. `border-inline-start` ולא `border-left`.
המערכת חייבת להיות ניתנת להיפוך לשפה LTR בלי לגעת ב-CSS.

### פריסת מסך תרגיל

זהה בכל היחידות. לא מסך מפוצל ולא שלושה טורים — **טור אחד ממורכז,
היררכיה אנכית:**

```
תגיות מצב (מיומנות · רמה · חזרה ממוקדת)
ההוראה
─────────────────────────────────────
   גוף השאלה — ביטוי, שרטוט או גרף
─────────────────────────────────────
שדה תשובה
סרגל סמלים (לפי ערכת היחידה)
כפתורי פעולה
משוב
```

השורה השלישית היא המקום הפנוי היחיד. באלגברה נכנס בה ביטוי, בגיאומטריה
שרטוט, בפונקציות גרף. **המסגרת לא משתנה.**

---

## מוטיבים ועומק

### רשת הנדסית — חוצת יחידות

רקע משבצות עדין, 20×20px, בעובי `rgba(0,0,0,0.05)`. מופיע ברקע מקטע הפתיחה
ובכרטיסי ויזואליזציה בלבד — **לא מתחת לטקסט קריאה**.

```css
background-image:
  linear-gradient(var(--grid-color) 1px, transparent 1px),
  linear-gradient(90deg, var(--grid-color) 1px, transparent 1px);
background-size: 20px 20px;
```

זהו המוטיב היחיד שמופיע בכל הספר, והוא מה שנותן לו זהות אחידה בין יחידות.

### חוזה המודל החזותי

במקום מוטיב אחד קבוע, המערכת מגדירה **תבנית**: לכל יחידה מודל חזותי קנוני
אחד, שעומד בארבעה תנאים:

1. **מסביר, לא מקשט.** הוא קיים כי הוא מבהיר מושג, לא כי צריך תמונה.
2. **SVG inline, בצבעי המערכת.** בלי תמונות חיצוניות, בלי צבעים חדשים.
3. **סטטי כברירת מחדל.** תנועה רק בהפעלה יזומה, וניתנת לחזרה.
4. **קריא במובייל** ברוחב 320px.

| במקור הסגנון | כאן |
|---|---|
| דיאגרמת מעגל | המודל החזותי של היחידה הנוכחית |
| הרכבת שבב | אנימציית בנייה של המודל, בהפעלה יזומה |
| ויזואליזציית נתונים | מפת השליטה של הלומד/ת |
| זוהר היי-טק | הילה `--electric` דקה סביב האלמנט הפעיל בלבד |

**דוגמה למילוי החוזה — יחידת האלגברה.** המודל שלה הוא מודל השטח: ריבוע
מחולק לארבעה מלבנים שמראה למה `(a+b)²` כולל `2ab`. הוא מרוויח את מקומו כי
הוא ההסבר החזותי לטעות הנפוצה ביותר באותה יחידה. **זו דוגמה, לא הגדרה** —
ביחידת פונקציות המודל יהיה מערכת צירים, ובגיאומטריה שרטוט מסומן.

### עומק

צל אחד בלבד בכל המערכת:

```css
--shadow-tech: 0 1px 2px rgba(0,0,0,.05), 0 8px 24px rgba(0,0,0,.05);
```

כרטיסים: `--surface` + מסגרת 1px `--border` + הצל. בלי הגבהות מדורגות.

---

## תנועה

- **פיזיקה:** ease-out, 200–300ms. חלק וצפוי.
- **כניסה:** fade + translateY(16px → 0) ב-420ms. ברשימות: מדרגה של 80ms.
- **ריחוף:** הסטת צבע עדינה + התאמת צל ב-200ms.
- **מעבר בין מסכים:** fade בלבד, 200ms.
- **ביצועים:** רק `transform` ו-`opacity`. שום מאפיין שמפעיל layout.

**שני חריגים פדגוגיים:**

1. **משוב על תשובה מופיע מיד, בלי אנימציית כניסה.** תלמיד/ה שממתין/ה
   לדעת אם צדק/ה לא צריך/ה לחכות 420ms.
2. **אנימציית בניית המודל היא הסבר, לא קישוט.** היא רצה רק בעמוד השיעור,
   רק בלחיצה יזומה, וניתנת להפעלה חוזרת.

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    transition-duration: .01ms !important;
  }
}
```

---

## צורות

רדיוס בסיס **8px**. כפתורים 8px, כרטיסים 12px, תגיות 999px (גלולה),
שדות קלט 8px. אין פינות חדות ואין עיגול מלא על מלבנים.

---

## רכיבים

כל הרכיבים חוצי-יחידות. אין רכיב שקיים רק בנושא אחד.

**כפתור ראשי** — מילוי `--blue-deep`, טקסט לבן, רדיוס 8px, משקל 600.
ריחוף: כהייה 8% + הרמת צל. לחיצה: `translateY(1px)`. בלי זוהר חיצוני.

**כפתור משני / רפאים** — מסגרת 1.5px `--border`, טקסט `--blue-primary`.
ריחוף: מילוי רקע עדין.

**כרטיס** — `--surface`, רדיוס 12px, מסגרת 1px `--border`, `--shadow-tech`.

**שדה קלט** — תווית מעל השדה. מסגרת 1px. פוקוס: טבעת 2px `--blue-primary`
במרווח 2px. שגיאה: טקסט מתחת. בלי floating labels.

**שדה תשובה מתמטית** — `--font-math`, 1.25rem, `dir="ltr"`,
`text-align: left`. יוצא דופן מכוון: זהו קלט LTR בתוך טופס RTL.
ה-`placeholder` מותאם לצורת התשובה הנדרשת ביחידה.

**סרגל סמלים — רכיב אחד, תוכן לפי יחידה.** המבנה, הגודל, יעד המגע וכלל
ה-`dir="ltr"` קבועים; רשימת התווים מגיעה מהצהרת היחידה.

- **התווית מראה מה הכפתור עושה, לא איזה תו הוא מקליד** — לכן `xⁿ` ולא `^`,
  ו-`√` ולא `sqrt`.
- מקסימום 12 מקשים בשורה. יותר מזה — לקבץ לשתי שורות לפי משמעות.
- כל כפתור: `dir="ltr"`, רוחב מינימלי 2.3rem, יעד מגע 44px.

**תגית מצב** — גלולה, 0.74rem, משקל 600. וריאנטים: ניטרלי, אזהרה, הצלחה.

**מחוון התקדמות** — גובה 6px, רדיוס מלא, רקע `--border`, מילוי
`--blue-primary` (או `--success` כשנשלט). מעבר 350ms על `width`.

**כרטיס מיומנות** — כותרת + תיאור + מחוון. שלושה מצבים:
פתוח (רגיל) · נשלט (מסגרת ורקע ירקרקים + תגית) · נעול (אטימות 55% +
**שורת הסבר: "נפתח אחרי: …"**). נעילה בלי סיבה אסורה.

**כרטיס יחידה** — כותרת היחידה, מספר המיומנויות שנשלטו מתוך הסך, מחוון,
ומצב פתוח/נעול. מבנה זהה לכרטיס מיומנות, ברמת היררכיה אחת מעליו.

**בלוק משוב** — רדיוס 12px, ריפוד 0.9rem, מסגרת 1px בגוון הסמנטי.
מבנה: כותרת מודגשת (שם האבחון) → הסבר → השאלה והתשובה.

**פאנל המורה הפרטי** — רקע `#ECEBF8`, מסגרת `#D2CFF0`, תווית עילית
"מורה פרטי" ב-0.75rem אותיות גדולות. מובחן חזותית מהמשוב הדטרמיניסטי,
כדי שיהיה ברור מה נקבע במנוע ומה נוסח על ידי מודל.

**בלוק שלב בשיעור** — תווית `שלב N:` בשורה נפרדת, `--blue-primary`,
0.82rem, משקל 700; התוכן בשורה שאחריה.
**אסור מספור אוטומטי של `<ol>`** — בשלבי פתרון מופיעים מספרים בתוך התוכן
(זוגות, נקודות ציון, מידות), ומספר עם נקודה לצידם נקרא כחלק מהמתמטיקה.

**בלוק נוסחה** — רקע `--ground`, מסגרת 1px, מסגרת פנימית 3px
`--blue-primary`, ממורכז, `--font-math` 1.25rem.

**בלוק איור** — מכל SVG ממורכז, רקע `--surface`, מסגרת 1px, כיתוב מתחת
ב-0.875rem `--ink`. קווי השרטוט ב-`--ink`, סימוני עזר ב-`--silver`,
הדגשות ב-`--blue-primary`. **בלי צבעים מחוץ לפלטה.**

**טבלה** — כותרות 0.8rem `--ink` בעמעום, גבול תחתון 1px, ריחוף שורה
ברקע `--ground`. בלי פסים מתחלפים.

**שלד טעינה** — אנימציית shimmer במידות הרכיב. **בלי ספינרים עגולים.**

**מצב ריק** — אייקון + טקסט מתאר + כפתור פעולה.

**אייקונים** — SVG inline בלבד, בסגנון Lucide/Heroicons, עובי קו 1.5–2px.
**אמוג׳י אסור כאייקון.**

---

## מסכי המוצר

זהו ספר לימוד, לא דף נחיתה. אין מקטעי מחירים, המלצות או קריאה לרכישה.

**1. כניסה** — כרטיס יחיד ממורכז, מקסימום 400px. כותרת + תת-כותרת + שני
שדות + כפתור. הכותרת הראשית של האפליקציה מוסתרת כאן — היא כבר בכרטיס.

**2. מפת הספר** — המסך הראשי. כרטיס פתיחה עם ברכה, אחוז התקדמות שנתי והצעד
הבא המומלץ. אחריו "נקודות שכדאי לסגור" (תפיסות מוטעות פעילות, חוצות יחידות),
ואז **היחידות** ככרטיסים. הרשימה גדלה לאורך השנה — הפריסה זורמת ואינה
מניחה כמות.

**3. מפת יחידה** — נפתחת מתוך כרטיס יחידה. המיומנויות מקובצות לשלבים, כל
שלב עם כותרת מיקרו. זהו המסך שהיה "מפת היחידה" כשהספר הכיל יחידה אחת.

**4. שיעור** — עמוד קריאה, טור 68–72ch. פתיח → בלוקי נוסחה, איורים, דוגמאות
פתורות ואזהרות → כפתור מעבר לתרגול.

**5. תרגול** — טור יחיד ממורכז לפי הפריסה שלמעלה, זהה בכל היחידות.

**6. משוב ואבחון** — מוצג מתחת לכפתורי הפעולה, בלי הזזת פריסה.

**7. מעקב כיתה** — שלושה כרטיסים בסדר הזה: *מה לעבור עליו בכיתה*
(תפיסות מוטעות מדורגות לפי שכיחות), *מיומנויות שהכיתה תקועה בהן*,
*טבלת תלמידים*. הסדר מכוון: מורה שואלת "על מה לעבור מחר", לא "מי קיבל כמה".
עם התרחבות הספר, שני הכרטיסים הראשונים מקבלים מסנן יחידה.

---

## נגישות

- ניגודיות טקסט 4.5:1 מינימום. שלושת הצבעים החסומים מפורטים בטבלת הצבעים.
- טבעת פוקוס נראית על **כל** אלמנט אינטראקטיבי: `outline: 2px solid
  var(--blue-primary); outline-offset: 2px`. אין `outline: none` בלי חלופה.
- יעד מגע מינימלי 44×44px, כולל כפתורי סרגל הסמלים.
- מצב לא מקודד בצבע בלבד — תמיד גם אייקון או טקסט.
- כותרות בסדר היררכי, בלי דילוג רמות.
- `lang="he"` ו-`dir="rtl"` על `<html>`.
- ביטויים מתמטיים, שרטוטים וגרפים מקבלים `aria-label` מילולי בעברית
  כשהצורה החזותית אינה מספיקה לקורא מסך. שרטוט ללא תיאור מילולי אינו
  נגיש, ובגיאומטריה זה קריטי.

---

## מותר ואסור

**אסור**
- אמוג׳י כאייקון
- שחור טהור `#000000` — להשתמש ב-`--ink`
- צבעים רוויי-יתר (תקרת רוויה 80%)
- **צבע ייעודי ליחידה או לנושא**
- **רכיב שקיים רק ביחידה אחת** — להרחיב רכיב קיים
- **ספירה קשיחה של יחידות או מיומנויות בעיצוב או ב-CSS**
- שלושה טורים שווים בפריסת מאפיינים — לטובת רשת א-סימטרית
- `h-screen` — להשתמש ב-`min-h-[100dvh]`
- קלישאות שיווקיות: "פורצת דרך", "חוויה חלקה", "הדור הבא"
- קישורי תמונה חיצוניים שבורים — SVG inline או placeholder מקומי
- lorem ipsum — תוכן מתמטי אמיתי בכל הדגמה
- CDN, Google Fonts, ספריות נוסחאות (KaTeX/MathJax)
- ספינרים עגולים
- מספור אוטומטי בשלבי פתרון
- הסימן `^` על המסך

**מותר ורצוי**
- רשת הנדסית עדינה — המוטיב המאחד של הספר
- מודל חזותי קנוני אחד לכל יחידה
- אנימציית בנייה של המודל, בהפעלה יזומה
- טיפוגרפיה טכנית מרוסנת
- מיקרו-אינטראקציות מדויקות
- ויזואליזציית מפת השליטה

---

## צ׳ק-ליסט להוספת יחידה

לפני שמתחילים לעצב יחידה חדשה, למלא את ארבעת הסעיפים:

- ☐ **מודל חזותי קנוני** — אחד, שעומד בארבעת תנאי החוזה
- ☐ **ערכת סמלים** — עד 12 מקשים, תווית לפי משמעות ולא לפי תו
- ☐ **צורות תשובה** — אילו מצבים הבודק מבחין ביניהם, ואיזה משוב לכל אחד
- ☐ **סוגי איורים** — ומה ה-`aria-label` שלהם

ואז לוודא:

- ☐ לא נדרש צבע חדש
- ☐ לא נדרש רכיב חדש
- ☐ לא נדרשה חריגה מהפריסה
- ☐ פריסת מסך התרגיל לא השתנתה

אם אחד מארבעת האחרונים נכשל — **החוזה שגוי וצריך לעדכן את המסמך הזה**,
לא לחרוג ממנו ביחידה אחת.

---

## טוקנים

```css
:root {
  /* צבע */
  --blue-deep:    #002D62;
  --blue-primary: #003366;
  --electric:     #00BFFF;
  --ink:          #36454F;
  --silver:       #C0C0C0;
  --border:       #E0E0E0;
  --surface:      #FFFFFF;
  --ground:       #F7F8FA;
  --success:      #008000;
  --success-bg:   #E4F3EA;
  --warning:      #FFA500;
  --warning-ink:  #8A5A00;
  --warning-bg:   #FBF0DA;
  --danger:       #A32036;
  --danger-bg:    #FBE9EC;
  --tutor-bg:     #ECEBF8;
  --grid-color:   rgba(0,0,0,.05);

  /* טיפוגרפיה */
  --font-he:   "Assistant", "Rubik", "Heebo", "Segoe UI", system-ui, sans-serif;
  --font-tech: "Roboto", "Assistant", system-ui, sans-serif;
  --font-math: "Cambria Math", "Latin Modern Math", Georgia, serif;
  --font-mono: "JetBrains Mono", ui-monospace, Consolas, monospace;

  /* מרווח — בסיס 8px */
  --space-1: .5rem;  --space-2: 1rem;   --space-3: 1.5rem;
  --space-4: 2rem;   --space-6: 3rem;   --space-8: 4rem;
  --section-gap: clamp(3rem, 6vw, 6rem);

  /* צורה */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-pill: 999px;
  --shadow-tech: 0 1px 2px rgba(0,0,0,.05), 0 8px 24px rgba(0,0,0,.05);

  /* מכלים */
  --container-wide: 1280px;
  --container-read: 72ch;

  /* תנועה */
  --ease: cubic-bezier(.16,1,.3,1);
  --dur-fast: 200ms;
  --dur-base: 300ms;
  --dur-enter: 420ms;

  /* שכבות */
  --z-nav: 100; --z-overlay: 200; --z-modal: 300; --z-toast: 500;
}
```

---

## מה הוסר ממקור הסגנון

המסמך נוקה מכל סממן מסחרי:

- הקרדיט וקישור החזרה לספק הסגנון, ושם המותג שלו
- מבנה דף הנחיתה המסחרי: מדרגות מחיר, המלצות לקוחות, קריאה לרכישה,
  אייקוני רשתות חברתיות וקישורי SEO בפוטר
- מסגור המוצר סביב שבבי 2nm ותעשיית המוליכים למחצה

מה שנשמר הוא הכיוון החזותי בלבד — פלטה, טיפוגרפיה, מבנה, תנועה ואיפוק —
מותאם לספר לימוד רב-יחידתי בעברית.
